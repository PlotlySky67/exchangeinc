# Secret used to authorize calls to /api/revalidate (the daily auto-refresh
# cron). Set this to any random string, both locally and in your hosting
# platform's environment variables. See README.md > "Automatic daily refresh".
CRON_SECRET=
